version: '3.8'

services:
  talaqqi-ai-server:
    build: .
    container_name: talaqqi_ai_server
    restart: always
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2048M
